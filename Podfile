platform :ios, '8.0'

use_frameworks!

def shared_pods
  pod 'RealmSwift', '~> 4.20.0'
  pod 'ObjectMapper', '~> 3.5.0'
end

target 'ObjectMapper_Realm' do
  shared_pods
end

target 'ObjectMapper_RealmTests' do
  shared_pods
end
